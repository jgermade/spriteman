# SVG Icons in Frame Actions & Remove Layer Pivot Button — Completed

## Goal / Context
1. Replace emoji icons in `.frame-actions-group` (`📋`, `🗑️`, `🧅`, `📦`) and `.active-group-pill` (`📍`, `✕`) with crisp, vector inline SVGs.
2. Remove the `📍 Pivote` button from the root layer and all layers in the Layer Hierarchy panel, reserving pivot interaction exclusively to the interpolation group menu (`.active-group-pill`).

## Implemented Changes
1. **SVG Icons for Frame Actions (`src/components/FrameStrip.ts`, `src/index.html`)**:
   - Replaced emoji `📋` with an SVG copy/duplicate icon (`<rect>` + `<path>`).
   - Replaced emoji `🗑️` with an SVG trash can icon.
   - Replaced emoji `🧅` with an SVG layered animation icon (`<polygon>` + `<polyline>`).
   - Replaced emoji `📦` with an SVG package / group box icon.
   - Replaced group pill emojis `📍` and `✕` with clean SVG crosshairs and close icons.
   - Styled `.btn-tool.icon-only svg` and `.btn-group-action svg` with `pointer-events: none; flex-shrink: 0; display: block;` to maintain perfect centering and seamless click delegation.
2. **Layer Hierarchy Cleanup (`src/components/HierarchyTree.ts`)**:
   - Completely removed `.btn-pivot-toggle` from all layers in `HierarchyTree`.
   - Layers now cleanly display only their drag handle, name, relative toggle (if child), and Z-index badge.
   - Pivot configuration is now activated exclusively from the interpolation group menu in the timeline reel.

## Verification
- Engine tests: `cargo test --manifest-path engine/Cargo.toml` passed 10/10 tests.
- Production build: `npm run build:web` built cleanly without warnings.
- End-to-end browser verification: Captured `ui_svg_icons_and_layers_1788693274741.png` confirming clean SVG vector icons in the timeline toolbar and total absence of the pivot toggle across all layers in the hierarchy panel.
