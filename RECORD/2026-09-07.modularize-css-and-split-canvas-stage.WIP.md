# Work Record: Modularize Component Styles & Split CanvasStage (WIP)

## 1. Goal / Context
1. Modularize `src/styles.css` by extracting component-specific CSS into co-located files alongside each component (e.g., `FrameStrip.css`, `HierarchyTree.css`, `PixelPalette.css`, `CanvasStage.css`, etc.), leaving `styles.css` with only global variables, resets, and layout imports.
2. Refactor `CanvasStage.ts` into a jq79 component `CanvasStage.html` (for HUD and viewport DOM) and `canvas.service.ts` (for coordinate calculations, transform state, and canvas rendering logic).

## 2. Planned Tasks
- Extract CSS chunks from `styles.css` into `src/components/*.css` and `src/views/EditorView.css`.
- Create `src/services/canvas.service.ts` for viewport transformations and canvas drawing.
- Create `src/components/CanvasStage.html` to mount the viewport and HUD.
- Update `src/views/EditorView.html` to mount `CanvasStage.html`.
- Remove legacy `CanvasStage.ts`.
- Verify build with `npm run build`.

## 3. Status
- WIP
