# Work Record: Unify EditorView into pure EditorView.html (WIP)

## 1. Goal / Context
Refactor `EditorView.ts` (740 lines) so that the root view is solely `src/views/EditorView.html`. Eliminate the legacy TypeScript class wrapper, allowing `EditorView.html` to be directly mounted by `src/index.html` via `EditorView.mount(document.getElementById('app'))`.

## 2. Planned Changes
1. **[src/views/EditorView.html](file:///Users/joshua/dev/joshua/spriteman/src/views/EditorView.html)**:
   - Embed all application coordination logic, services bindings, lifecycle, keyboard shortcuts, and child component initializations inside `<script :setup type="text/typescript">`.
   - On `$mounted()`, initialize `CanvasStage` and mount all child `.html` components (`ProjectTabs`, `WelcomeScreen`, `HierarchyTree`, `FrameStrip`, `PixelPalette`, `InspectorPanel`, `AnimationSettings`, `ProjectModal`).
   - Wire all service subscriptions (`tabsService`, `animationService`, `projectService`, `pwaService`) directly into local reactive properties.
2. **[src/index.html](file:///Users/joshua/dev/joshua/spriteman/src/index.html)**:
   - Import `EditorView from './views/EditorView.html'` directly in `<script type="module">`.
   - Call `EditorView.mount(app)` instead of instantiating `new EditorView()`.
3. **Delete [src/views/EditorView.ts](file:///Users/joshua/dev/joshua/spriteman/src/views/EditorView.ts)**.
4. **Verification**:
   - Verify build with `npm run build`.
   - Test in browser with `browser_subagent`.

## 3. Status
- WIP
