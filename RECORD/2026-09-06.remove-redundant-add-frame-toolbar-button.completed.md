# Remove Redundant '+ Add Frame' Button From Toolbar — Completed

## Goal / Context
Remove the `+ Add Frame` (`.btn-tool.btn-add`) button from `.frame-actions-group` in the timeline toolbar, as it is redundant with the interactive `+` cell (`.frame-cell.new`) already situated directly in the frame reel.

## Implemented Changes
1. **FrameStrip Component (`src/components/FrameStrip.ts`)**:
   - Removed `<button class="btn-tool btn-add primary"> + Add Frame </button>` from `.frame-actions-group`.
   - Removed the corresponding `.btn-add` click event listener.
   - Preserved `Duplicate`, `Delete`, and `🧅 Onion Skin`, as well as `📦 Agrupar` (when a range of frames is selected).
   - The `+` cell (`.frame-cell.new`) at the end of `.frames-reel` handles adding frames on click or dragging to insert frames at any position between existing cells.

## Verification
- Engine tests: `cargo test --manifest-path engine/Cargo.toml` passed 10/10 tests.
- Vite build: `npm run build:web` built production bundle cleanly.
- End-to-end browser subagent verification:
  - Verified `+ Add Frame` is absent from `.frame-actions-group`.
  - Verified `Duplicate`, `Delete`, and `🧅 Onion Skin` remain active and functional.
  - Verified clicking the `+` frame cell in `.frames-reel` appends a new frame (tested: frame count increased from 27 to 28).
  - Screenshot artifact: `timeline_toolbar_reel_1788690940959.png`.
