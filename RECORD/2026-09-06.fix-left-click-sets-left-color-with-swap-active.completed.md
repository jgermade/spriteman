# Work Record: Left-Click Sets Left Color When Swap Colors Is Active

## 1. Goal / Context
Ensure that left-clicking any color swatch in `PixelPalette` always sets the left color slot (**Color I** / `primaryColor`), including when `btn-swap-colors` is active (e.g. while holding `Alt`/`Meta` for temporary color swapping).

## 2. Tasks & Changes
- **[src/components/PixelPalette.html](file:///Users/joshua/dev/joshua/spriteman/src/components/PixelPalette.html)**:
  - In `handleClick`, removed `e.metaKey || e.altKey` diverting left clicks to `onSelectSecondary`.
  - Replaced modifier logic so that standard Left-Click always sets `Color I` (`primaryColor`), while `Shift + Click` or `Right-Click` (`contextmenu`) sets `Color D` (`secondaryColor`).
  - When `btn-swap-colors` is active (holding Alt/Cmd or toggled), left-clicking a swatch updates `Color I` (the left slot), allowing the user to pick a new color for that slot before releasing the swap modifier.

## 3. Verification
- Compiled and bundled with `npm run build` (`wasm-pack` and `vite build`).
- Ran automated browser subagent test:
  - Verified initial state: `Color I: #000000`, `Color D: #ffffff`.
  - Activated `btn-swap-colors`.
  - Left-clicked on cyan swatch (`#5fcde4`): `Color I` immediately updated to cyan.
  - Deactivated swap / restored: Verified color slots retained updated definitions cleanly.
  - 0 console errors.

## 4. Status
- Completed.
