# Work Record: Remove Redundant Topbar Undo/Redo Buttons

## 1. Goal / Context
Remove `#btn-topbar-undo` and `#btn-topbar-redo` from `.topbar-right`. These buttons were redundant because undo and redo actions are already prominently available in the bottom timeline `FrameStrip` toolbar (`.btn-undo` and `.btn-redo`) and through standard keyboard shortcuts (`Cmd+Z` / `Ctrl+Z` and `Cmd+Shift+Z` / `Ctrl+Shift+Z`).

## 2. Tasks & Changes
- **EditorView Template ([src/views/EditorView.html](file:///Users/joshua/dev/joshua/spriteman/src/views/EditorView.html))**:
  - Removed `#btn-topbar-undo` and `#btn-topbar-redo` buttons from `.topbar-right`.
  - Cleaned up `canUndo`, `canRedo`, `onUndo`, and `onRedo` props from `<script :setup>`.
- **EditorView Controller ([src/views/EditorView.ts](file:///Users/joshua/dev/joshua/spriteman/src/views/EditorView.ts))**:
  - Cleaned up unneeded topbar bindings from `this.component.mount` and state listeners.
  - Retained `projectService.canUndo()` and `projectService.canRedo()` passed to `FrameStrip` timeline component.

## 3. Verification
- Compiled and bundled with `npm run build` (`wasm-pack` and `vite build`) without warnings.
- Ran automated browser subagent to verify `topbar-right` layout and DOM state:
  - Confirmed `btn-topbar-undo` and `btn-topbar-redo` are removed.
  - Confirmed `💾 Save` and optional `⬇ Install App` buttons are cleanly displayed in `topbar-right`.
  - Confirmed timeline toolbar undo/redo buttons remain fully operational.

## 4. Status
- Completed.
