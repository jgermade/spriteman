# Frame Actions Group Icon-Only Buttons — Completed

## Goal / Context
Convert all buttons in `.frame-actions-group` to clean, icon-only representations without text labels, providing a more compact and elegant timeline toolbar.

## Implemented Changes
1. **FrameStrip Component (`src/components/FrameStrip.ts`)**:
   - `btn-duplicate`: Replaced text "Duplicate" with `📋` (title: "Duplicar fotograma actual").
   - `btn-delete`: Replaced text "Delete" with `🗑️` (title: "Eliminar fotograma actual").
   - `btn-onion`: Replaced text "🧅 Onion Skin" with `🧅` (title: "Papel cebolla (Onion Skinning)").
   - `btn-group-selection`: Replaced text with `📦` (title: "Agrupar fotogramas seleccionados (${rangeStart + 1}..${rangeEnd + 1}) con pivote e interpolación automática").
   - Added `icon-only` class to each button.
2. **Styling (`src/index.html`)**:
   - Added `.btn-tool.icon-only` CSS rule with centered flex layout (`min-width: 28px; height: 26px; padding: 0 0.35rem; font-size: 0.82rem;`).

## Verification
- Engine tests: `cargo test --manifest-path engine/Cargo.toml` passed 10/10 tests.
- Vite build: `npm run build:web` compiled cleanly.
- End-to-end browser subagent verification:
  - Verified `btn-duplicate` shows only `📋`.
  - Verified `btn-delete` shows only `🗑️`.
  - Verified `btn-onion` shows only `🧅`.
  - Verified `btn-group-selection` shows only `📦`.
  - Verified toggling `🧅` works and highlights correctly.
  - Screenshot artifact: `timeline_toolbar_clean_icons_1788691161269.png`.
