# Record: Replace Checkerboard with Pixel Grid Only

- **Date**: 2026-09-06
- **Status**: WIP
- **Task Summary**: Replace the transparency checkerboard (damero) with a clean solid artboard background and a dedicated 1:1 pixel grid ("sólo rejilla").

---

## 1. Goal / Context

The user specified:
> "en lugar del damero, mejor sólo rejilla"

Instead of alternating checkerboard squares, the user prefers a clean, solid artboard with only the pixel grid lines demarcating each discrete 1:1 pixel cell.

---

## 2. Planned Changes

1. **CanvasStage Component ([CanvasStage.ts](file:///Users/joshua/dev/joshua/spritemotion/src/components/CanvasStage.ts))**:
   - In `redraw()`:
     - Remove `drawPixelCheckerboard`.
     - Fill the sprite artboard bounds with a solid clean dark slate canvas color (`#0d131d`).
     - Render `drawPixelGrid` directly across the sprite bounds with crisp pixel lines.
     - Retain outer border `#38bdf8`.
     - Overlay subtle grid lines when zoomed in so pixels retain cell demarcation.
2. **Verification**:
   - Verify component isolation.
   - Run `npm run build` and `cargo test`.
   - Verify visually in browser subagent.
