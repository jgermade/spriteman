# Superimpose Active Group Pill On Intermediate Frames — Completed

## Goal / Context
Superimpose `.active-group-pill.in-reel` directly on top of the intermediate frame buttons in the reel, keeping the frame cells at a uniform 54px height and providing an overlay bridge with glassmorphism and centered icon-only buttons.

## Implemented Changes
1. **Layout & Positioning (`src/index.html`)**:
   - Set `.group-intermediate-wrapper` to `position: relative; display: inline-flex; align-items: center; height: 54px;`.
   - Maintained `.group-intermediate-cells` at `height: 54px; display: flex; align-items: center; gap: 0.45rem;`.
   - Positioned `.active-group-pill.in-reel` absolutely with `top: 50%; left: 50%; transform: translate(-50%, -50%); z-index: 10;`.
   - Styled the pill with rich glassmorphism: `background: rgba(15, 23, 42, 0.92); backdrop-filter: blur(8px); border: 1.5px solid #818cf8; border-radius: 6px; box-shadow: 0 4px 16px rgba(0, 0, 0, 0.65), 0 0 12px rgba(99, 102, 241, 0.45);`.
   - Centered group title (`⚡ Grupo`) and icon-only action buttons (`📍` and `✕`).
2. **Intermediate Frames Appearance**:
   - Styled `.frame-cell.group-intermediate` with `opacity: 0.7; border: 2px dashed rgba(99, 102, 241, 0.45);` and lock indicator `🔒`, clearly indicating that intermediate frames are automatically interpolated by the group.

## Verification
- Engine unit & integration tests: `cargo test --manifest-path engine/Cargo.toml` passed 10/10 tests.
- Vite build: `npm run build:web` built production bundle cleanly without warnings.
- End-to-end browser subagent verification:
  - Verified `.active-group-pill.in-reel` is cleanly superimposed on top of intermediate frame buttons.
  - Verified the reel height remains completely uniform at 54px without any vertical displacement.
  - Verified the action buttons display exclusively icons (`📍` and `✕`) with active styling on click.
  - Screenshot artifact: `frame_reel_final_verification_1788691556169.png`.
